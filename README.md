```
$ whoami
```
**Milad Shaddelan** — Data Engineer at a hedge fund. Building AI development infrastructure on the side.

```
$ cat ~/.thesis
```
AI can generate code. But generating code isn't shipping software.
The industry talks about 10x productivity, but the most rigorous research shows
developers *feel* 20% faster while actually working 19% slower.
The missing layer is orchestration — planning, validation, quality gates —
the infrastructure between "AI wrote it" and "it's in production."

That's what I build.

```
$ ls ~/shipping
```
| Project | What it does |
|---------|-------------|
| **qalib** | AI dev orchestration — PM, Coder, and QC agents that plan, build, and validate code autonomously |
| **lynurl** | Full-stack URL shortener — Flutter + Cloudflare Workers + Redis |
| *...more* | *2026 is going to be busy* |

```
$ cat /etc/stack
```
`Python` `TypeScript` `FastAPI` `Next.js` `Flutter` `Docker` `PostgreSQL` `Redis` `Claude AI` `AWS`

```
$ cat ~/.services
```
I help developers and teams ship products with AI — quick, secure, and cheap.
If you're building with AI agents or trying to get your eng team to actually ship faster
with LLMs (not just generate more code), let's talk.

```
$ open ~/links
```
[LinkedIn](https://www.linkedin.com/in/miladshaddelan) | [GitHub](https://github.com/MiladShd)
